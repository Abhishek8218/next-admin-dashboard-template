import { atom } from "recoil";

export const fileKey = atom({
    key: 'fileKey',
    default: '',
    });

export const fileContentType = atom({
    key: 'fileContentType',
    default: '',
    });
    